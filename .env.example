DATABASE_URL=postgresql://postgres:postgres@localhost:5432/eve_trade_v2
HOST=0.0.0.0

# Codespaces bootstrap ports. The applications themselves receive PORT per process.
API_PORT=3000
WEB_PORT=3001

# Required only when the worker is launched explicitly.
ESI_USER_AGENT=EVE-Trade-v2/0.1
